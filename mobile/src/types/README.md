# Types

Mirrors backend domain entities for type-safe API responses:

- **product.ts** — Product, ProductFilters
- **cart.ts** — Cart, CartItem
- **order.ts** — Order, OrderItem, OrderStatus, Address
